# LifeLine - Example Environment
# Copy this file to .env and fill in values.

# --- Core ---
FLASK_DEBUG=0
PORT=5000

# Flask auth/session
SECRET_KEY=change-me
JWT_SECRET_KEY=change-me

# Cookies (set to 1 in production when served over HTTPS)
SESSION_COOKIE_SECURE=0

# --- Database ---
# If not set, the app uses a local SQLite database at ./lifeline.db
# DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DBNAME

# --- Smart Suggestions (optional) ---
# OpenWeatherMap API (Get a free key from https://openweathermap.org/api)
OPENWEATHER_API_KEY=

# Fallback coordinates used when user location is unavailable
DEFAULT_LAT=23.8103
DEFAULT_LNG=90.4125

# Set to 0 to ensure real weather is used (1 enables demo fallback)
DEMO_WEATHER=0

# Suggestion radius (in kilometers)
SUGGESTION_RADIUS_KM=5

# --- Translation (optional) ---
USE_GOOGLE_CLOUD_TRANSLATE=0

# --- Maps (recommended for map + radar UI) ---
# Google Maps (browser key; restrict by HTTP referrer in Google Cloud Console)
GOOGLE_MAPS_API_KEY=

# --- Email (optional; Gmail SMTP used by default) ---
EMAIL_ADDRESS=
EMAIL_PASSWORD=
MAIL_TIMEOUT=10

# --- Firebase Admin / Push Notifications (optional) ---
# Server-side credentials (DO NOT commit service account JSON)
# Recommended for Render: paste JSON in env var (single line) or base64 it.
FIREBASE_SERVICE_ACCOUNT_JSON=
FIREBASE_SERVICE_ACCOUNT_JSON_BASE64=

# Optional: path to JSON if you mount/upload a secret file
FIREBASE_SERVICE_ACCOUNT_PATH=

# Or rely on standard Google ADC:
GOOGLE_APPLICATION_CREDENTIALS=
